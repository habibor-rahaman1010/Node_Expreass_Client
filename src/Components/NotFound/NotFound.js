import React from 'react';
import { Container } from 'react-bootstrap';

const NotFound = () => {
    return (
        <Container>
            <h1>Not found the page please try again!</h1>
        </Container>
    );
};

export default NotFound;